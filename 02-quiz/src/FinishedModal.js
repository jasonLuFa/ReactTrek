import { useState } from 'react';

const FinishedModal = () => {
  const [isFinishedModalOpen, setIsFinishedModalOpen] = useState(false);

  return <div>FinishedModal</div>;
};

export default FinishedModal;
